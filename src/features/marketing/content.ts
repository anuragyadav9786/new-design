// Canonical marketing-content entry point.
// Kept as a compatibility boundary during the redesign migration so UI output remains identical.
export {
  goalVisuals,
  trustStats,
  processSteps,
  productShowcaseSteps,
  ecosystemPillars,
  testimonials,
  insights,
  faqs,
} from "@/components/redesign/data";
